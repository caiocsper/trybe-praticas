// Insert explanation here
let     height      = 8;

const   base        = 5;
const   area        = base * height;
const   perimeter   = (base + height) * 2;

console.log("Altura do retângulo: "     + height);
console.log("Base do retângulo: "       + base);
console.log("Área do retângulo: "       + area);
console.log("Perímetro do retângulo: "  + perimeter);